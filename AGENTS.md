# Agents

Read [`DESIGN.md`](./DESIGN.md) before UI work.

Night research strip — IBM Plex, scarce amber, narrow measure.

**Definition (E):** a meta-harness is an **operator OS for a fleet of agents** — define agents/roles, assign work, isolate permissions, walk away; runtime/harness underneath is swappable.

**Research spine (source of truth for content)**
- [`FEATURES.md`](./FEATURES.md) — feature catalog
- [`LANDSCAPE.md`](./LANDSCAPE.md) — complete project list + layers
- [`FEATURE_MAP.md`](./FEATURE_MAP.md) — feature → project → approach (prose, not Y/N)
- [`CANDIDATES.md`](./CANDIDATES.md) — who is core OS vs demoted

**Content**
- Essay + feature approaches from primary sources. No coverage-matrix soup. No organ chips.
- Do not invent a coy private self-slot on the page. Do not name private stack products here.
- Vertical (coding / ops / CX) does **not** decide membership.

**Layer hints (claim, not vertical)**
- **os-candidate / shop** — full operator OS over muscle (see CANDIDATES core list). HappyRobot is in.
- **control** — run/session/HITL/deploy APIs (Agno AgentOS, LangSmith Deployment). Not the OS product.
- **framework** — libraries that build the loop (LangGraph, LangChain, deepagents).
- **observe** — traces/evals (LangSmith).
- **work / fleet / retrieve** — single-organ or fleet surfaces. Fleet includes multiplayer ADEs (QM, AQ) and CRHQ — still not a full OS unless multi-organ operator claim.
- **substrate** — isolates, sandboxes, agent computers under the muscle. Not the shop.
- **optimize** — searches harness *code*.
- **adjacent** — nearby product that does **not** claim the OS (HQ = company brain, Sierra, Agentforce, OpenWorker, Hermes).
- **reference** — blueprints only (Postma/Ian Nuttall AgentOS gist).
